# --- Gemini (Google AI Studio key — no GCP IAM needed) ---
# Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Set USE_MOCK=false to call Gemini.
USE_MOCK=true

PORT=8000
